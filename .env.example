DATABASE_URL="postgresql://lot_agent:lot_agent@localhost:5432/lot_agent"

# Powers the chat widget (app/api/chat) and scripts/rotate-car-inventory.ts.
# Get one at https://console.anthropic.com — without it, the chat widget
# degrades gracefully (shows an error in the UI, doesn't crash the app).
ANTHROPIC_API_KEY=
